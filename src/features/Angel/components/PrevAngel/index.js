import styled from 'styled-components';

const PrevAngel = styled.div`
  position: absolute;
  top: 50%;
  transform: translate(-30%, -50%);
  left: 0;
  filter: blur(5px);
  opacity: 0.65;
`;

export default PrevAngel;
