import styled from 'styled-components';

const NextAngel = styled.div`
  position: absolute;
  top: 50%;
  right: 0;
  transform: translate(30%, -50%);
  filter: blur(5px);
  opacity: 0.65;
`;

export default NextAngel;
