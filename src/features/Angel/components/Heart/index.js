import styled from 'styled-components';

const Heart = styled.img`
  width: 2.6875rem;
  height: auto;
  position: absolute;
  top: -0.42rem;
  left: -0.7rem;
  cursor: pointer;
  z-index: 999;
`;

export default Heart;
