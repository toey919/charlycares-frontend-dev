import styled from 'styled-components';

const DateTimeValues = styled.span`
  font-family: ${props => props.theme.primaryFont};
  font-size: 1rem;
  color: ${props => props.theme.lightGrey};
`;

export default DateTimeValues;
